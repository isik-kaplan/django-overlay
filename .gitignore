# Python
__pycache__
*.pyc
.pytest_cache
.ruff_cache
.coverage
htmlcov
.hypothesis

# mutmut — the mutated copy of the tree plus its cached per-mutant results
mutants
.mutmut-cache
mutmut-stats.json

# Saved benchmark runs. Machine-specific by construction — a run is only
# comparable against another taken on the same hardware, Postgres version and
# work_mem — so committing one would invite comparisons that mean nothing.
benchmark/results/

# IDEs & Editors
.idea

# Frameworks & Libraries
dist
poetry.lock

# venv
.venv

# Project Specific
# ETC
